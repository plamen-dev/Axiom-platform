# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.env
.venv
env/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# Type checking
.mypy_cache/
.dmypy.json
dmypy.json

# Documentation
docs/_build/

# OS
.DS_Store
Thumbs.db

# Execution logs (top-level only)
/logs/

# Test harness artifacts
artifacts/grid_test_runs/
artifacts/level_test_runs/

# Generated Axiom artifacts
artifacts/inventory_plans/
artifacts/inventory_runs/
artifacts/local_runner_runs/
artifacts/model_inventory_runs/
artifacts/object_registry_candidates/
artifacts/parameter_registry_candidates/
artifacts/parameter_edit_runs/
artifacts/scratch/
# Validation Automation Loop runs (generated run outputs only; keep curated reports)
artifacts/validation_runs/vrun_*/
# Validation Evidence Runner bundles (generated run outputs; keep curated evidence)
artifacts/validation_evidence/
# Capability Execution Runner bundles (generated run outputs; keep curated evidence)
artifacts/capability_runs/
# Promotion Eligibility Engine checks (generated decision records; keep curated evidence)
artifacts/promotion_checks/
# PR snapshot artifacts (generated CI evidence bundles)
artifacts/pr_snapshots/
# Execution Outcome Framework bundles (generated run outputs; keep curated evidence)
artifacts/execution_outcomes/
# Failure Classification Framework bundles (generated run outputs; keep curated evidence)
artifacts/failure_classifications/
# Recovery Recommendation Framework bundles (generated run outputs; keep curated evidence)
artifacts/recovery_recommendations/
# Recovery Execution Framework bundles (generated run outputs; keep curated evidence)
artifacts/recovery_executions/
# Session Memory Framework bundles (generated run outputs; keep curated evidence)
artifacts/session_memory/
# Skill Composition Framework bundles (generated run outputs; keep curated evidence)
artifacts/skill_compositions/
# Capability Routing Framework bundles (generated run outputs; keep curated evidence)
artifacts/capability_routing/

# Capability Selection Framework bundles (generated run outputs; keep curated evidence)
artifacts/capability_selection/

# Capability Chain Framework bundles (generated run outputs; keep curated evidence)
artifacts/capability_chains/

# Global Capability Registry Framework bundles (generated run outputs; keep curated evidence)
artifacts/global_capability_registry/

# Capability Event Timeline Framework bundles (generated run outputs; keep curated evidence)
artifacts/capability_event_timeline/

# GitHub Metadata Import Framework bundles (generated run outputs; keep curated evidence)
artifacts/github_metadata_import/

# Capability Summary Framework bundles (generated run outputs; keep curated evidence)
artifacts/capability_summary/

# Devin Session Metadata Import Framework bundles (generated run outputs; keep curated evidence)
artifacts/devin_session_import/

# Capability Relationship Framework bundles (generated run outputs; keep curated evidence)
artifacts/capability_relationships/

# Capability Impact Framework bundles (generated run outputs; keep curated evidence)
artifacts/capability_impacts/

# Capability File Knowledge Framework bundles (generated run outputs; keep curated evidence)
artifacts/capability_file_knowledge/

# Capability Validation Knowledge Framework bundles (generated run outputs; keep curated evidence)
artifacts/capability_validation_knowledge/

# Capability Knowledge Graph Framework bundles (generated run outputs; keep curated evidence)
artifacts/capability_knowledge_graph/

# Execution Context Framework bundles (generated run outputs; keep curated evidence)
artifacts/execution_context/

# Execution Environment Framework bundles (generated run outputs; keep curated evidence)
artifacts/execution_environment/

# Execution Resource Framework bundles (generated run outputs; keep curated evidence)
artifacts/execution_resource/

# Execution Constraint Framework bundles (generated run outputs; keep curated evidence)
artifacts/execution_constraint/

# Visual Studio local state
.vs/

# Local documentation backups
docs-local-backup-*/

# Project specific
*.rvt
*.rfa
*.rte
*.rft
